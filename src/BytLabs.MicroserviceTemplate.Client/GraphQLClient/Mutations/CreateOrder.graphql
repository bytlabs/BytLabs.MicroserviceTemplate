mutation CreateOrder($input: CreateOrderInput!) {
  createOrder(input: $input) {
    createOrderResult {
      orderId
    }
    errors {
      ... on BusinessError {
        code
        property
        message
      }
      ... on ValidationError {
        message
        fields {
          field
          code
          message
        }
      }
    }
  }
}
