mutation CreateProduct($input: CreateProductInput!) {
  createProduct(input: $input) {
    product {
      id
      name
    }
    errors {
      ... on BusinessError {
        code
        property
        message
      }
      ... on ValidationError {
        message
        fields {
          field
          code
          message
        }
      }
    }
  }
}
