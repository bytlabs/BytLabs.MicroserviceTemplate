mutation CreateEntityDef($input: CreateEntityDefInput!) {
  createEntityDef(input: $input) {
    entityDef { id entityType form { key } }
    errors {
      ... on BusinessError { code property message }
      ... on ValidationError { message fields { field code message } }
    }
  }
}
